local _ = require("gettext")

return {
    name = "folizen",
    fullname = _("Folizen"),
    description = _("Sync reading progress, highlights, notes, and vocabulary to your Folizen library (one-way, device to server)."),
}
